# Kept for compatibility; primary CI lives in .github/workflows/ci.yml
sudo: false
language: node_js
node_js:
  - "20"
cache:
  directories:
    - node_modules
install:
  - npm ci
script:
  - npm run build:ci
git:
  depth: 1
